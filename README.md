# arclineads.com

The ArcLine landing page: one static file, hosted on GitHub Pages.

- Edit `index.html`, push to `main`, the site updates within a minute.
- `CNAME` holds the custom domain; DNS at the registrar points to GitHub Pages.
